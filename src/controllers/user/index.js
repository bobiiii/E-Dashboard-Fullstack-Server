const {
  getUsers, deleteUser, addUserController, loginUserController,
} = require('./user.controllers');

module.exports = {
  getUsers,
  deleteUser,
  addUserController,
  loginUserController,
};
