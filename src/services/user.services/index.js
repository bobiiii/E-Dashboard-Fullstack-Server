const {
  addUser, deleteUser, loginUser, getUserEmail, getUserId, getUsers,
} = require('./user.services');

module.exports = {
  addUser,
  deleteUser,
  loginUser,
  getUserEmail,
  getUserId,
  getUsers,
};
